/var/log/mongodb/mongod-<%= @name %>.log {
    daily
    missingok
    rotate 14
    compress
    postrotate
        /bin/kill -SIGUSR1 `cat /var/run/mongodb-<%= @name %>.pid`
    endscript
    notifempty
    nocreate
}